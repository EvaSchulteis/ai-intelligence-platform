[project]
name = "ai-intelligence-platform"
version = "0.1.0"
description = "A Python project for collecting data on the AI ecosystem."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
